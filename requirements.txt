# MLOps Fraud Detection Template - Production Dependencies
# 
# Version Pinning Strategy:
# - Using ~= (compatible release) to allow patch updates but prevent breaking changes
# - Example: pandas~=1.5.0 allows 1.5.x but not 1.6.0
#
# For production use, consider pinning exact versions (==) for full reproducibility
# 
# Development dependencies (testing, linting, notebooks) are in requirements-dev.txt

## Core Data Science Libraries
pandas~=2.0.0          # Data manipulation and analysis
numpy>=1.23.0,<2.0.0   # Numerical computing (compatible with all libs)
scikit-learn~=1.3.0    # Machine learning algorithms and utilities
xgboost~=2.0.0         # Gradient boosting (optional, can be disabled in config)

## Visualization Libraries (for model evaluation plots)
matplotlib~=3.8.0      # Basic plotting for evaluation metrics
seaborn~=0.13.0        # Statistical visualization for confusion matrices

## MLOps and Experiment Tracking
mlflow~=2.7.0          # Experiment tracking, model registry, and deployment

## API and Web Framework
fastapi~=0.100.0       # Modern web framework for building APIs
uvicorn[standard]~=0.23.0  # ASGI server for FastAPI
pydantic~=2.0.0        # Data validation and settings management

## Configuration and Data Processing
pyyaml~=6.0            # YAML configuration file parsing
python-multipart~=0.0.6  # Form data parsing for FastAPI

## HTTP Client Libraries
requests~=2.31.0       # HTTP library for API testing
httpx~=0.24.0          # Modern async HTTP client

## Optional Production Dependencies
# Uncomment if needed for specific production features:
# prometheus-client~=0.17.0  # For Prometheus metrics export

